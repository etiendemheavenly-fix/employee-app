apiVersion: v2
name: employee-app
description: Employee Directory application with Flask backend, Nginx frontend, and monitoring
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "72.6.2"
    repository: https://prometheus-community.github.io/helm-charts
    condition: monitoring.enabled
